let initialState = {
    friendsList: [
        { id: 1, name: 'Zorro' },
        { id: 2, name: 'Lyubov' },
        { id: 3, name: 'Kuscherenko' }
    ]
};

let sideBarReducer = (state = initialState, action) => {
    return state;
}
export default sideBarReducer;